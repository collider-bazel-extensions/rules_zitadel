"""Values files exported so consumers can inspect / extend them."""

package(default_visibility = ["//visibility:public"])

exports_files([
    "zitadel-values.yaml",
    "zitadel-external-pg-values.yaml",
])
